import EmptyState from "@/components/empty-state"

const Chat = () => {
      return (
            <div className="hidden lg:block h-svh">
                  <EmptyState />
            </div>
      )
}

export default Chat